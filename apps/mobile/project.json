{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mobile",
  "projectType": "application",
  "sourceRoot": "apps/mobile",
  "tags": ["platform:mobile", "scope:app"],
  "targets": {
    "serve": {
      "command": "expo start --localhost",
      "continuous": true,
      "options": {
        "cwd": "apps/mobile",
        "args": ["--clear"]
      }
    }
  }
}
